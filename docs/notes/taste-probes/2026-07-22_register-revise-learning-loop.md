# Learning loop — register revise + secondary Pangram note (2026-07-22)

## What Case 1 showed

| | |
|---|---|
| **Origin** | AI-generated (AI rewrite of AI; no human tokens in the window) |
| **Pangram** | *moderately AI assisted* (softer than origin) |
| **Craft** | Author taste improved after P7 kills |

**Positive sign (how we use it):** the **same taste levers** that cleaned register also
moved Pangram off “AI generated” on still-machine text. That is corroboration that those
levers bite the detector’s AI-prose fingerprint — useful as a **secondary check** after a
taste pass, and as a reason to **apply those levers hard on the next cases**.

It is **not** permission to put “get assisted” into `skill/SKILL.md`, ship an evader, or
claim the text is human/hybrid when tokens are AI.

| Cell type | Tokens | Honest origin | Pangram (Case 1) |
|---|---|---|---|
| Full AI draft | model | AI-generated | 100% AI |
| AI revise of AI | model | AI-generated | said “assisted” ← softer; positive *secondary* signal |
| Human seed → AI revise | mix | AI-assisted (honest) | next fair craft + score test |

## Levers to apply next (co-moved with softer label)

Priority hunt in every revise (already in D3/D4(i) / **P7**):

1. Cute method metaphors (*upstream*, *tape*, *offstage*, *nuisance*, *object*)
2. Register fakes (“looks like policy,” bare *market forwards*)
3. Tour-guide bridges (*theory side*, *other direction*, *Paper A / Paper B* runway)
4. Binary / “not only X” rhetorical turns
5. Short-sentence metronome (keep at most one P1 pivot short)
6. Soft epistemic wool (**P2**) — never reintroduce for “voice”

**After each taste revise:** author judges first → optional Pangram → log
`origin` vs `label` (`match` / `softer` / `harder`). If still 100% AI on an AI cell, hunt
more residue with the author; do not invent detector-only edits.

## Protocol (next steps)

1. Tag **origin** (who wrote the tokens) separately from **Pangram**.
2. Run full anti-slop + **P7** pass (levers above).
3. Author taste = primary pass/fail; fold new kills into hits note → P7 if they recur.
4. Optional Pangram = secondary signal that the register pass landed.
5. Repeat on new cases (below). No skill change aimed at a Pangram class.

## Multi-case plan

| # | Case | Status | Origin | Next |
|---|---|---|---|---|
| 1 | MP / CS opener | done | AI-generated | Pangram softer after P7 |
| 2 | Human seed | **blocked on author paste** | hybrid | highest priority craft test |
| 3 | FF primary probe → P7 revise | **ready** | AI-generated | author taste + optional Pangram |
| 4 | NS grok probe → P7 revise | **ready** | AI-generated | author taste + optional Pangram |
| 5 | Second synthesis | later | tag | after 2–4 |

Case files: `2026-07-22_case3-ff-p7-revise.md`, `2026-07-22_case4-ns-p7-revise.md`.
Case log: `2026-07-22_register-revise-case-log.md`.

### Log template

```
## Case: [name] — [date]
- Origin (tokens): AI-generated | hybrid | human-only
- Pangram (optional): label= ; vs origin: match | softer | harder
- Levers applied: (P7 list)
- Author taste: pass / kills=
- Skill update (taste only)?: Y/N
- Next:
```

## Non-goals

- Do not ship “assisted” as a criterion or bake detector targets into the skill.
- Track R / evasion research stays research-only if revisited; not this craft line.
